const dashboardHandler = (req, res) => {
  // logic
  res.write("Success....!");
  res.end();
};

const registerHandler = (req, res) => {
  // logic
};

const loginHandler = (req, res) => {
  // logic
};

module.exports = {
  dashboardHandler,
  registerHandler,
  loginHandler,
};
